# Fibrin_Platelet_Cmake
After forking the code, put on crc. 
then:
module load cmake
navigate to the build/Linux-Debug folder
type cmake ../../src/Linux-Debug to configure the CMakeLists.txt
the while in build folder type "make"
